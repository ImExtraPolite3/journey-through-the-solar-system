import './style.css';
import {
  addStars,
  animateTitle,
  horizonatalScroll,
  moveStars,
  moveToNext,
} from './startPage';

animateTitle();
addStars();
moveStars();
moveToNext();
horizonatalScroll();
