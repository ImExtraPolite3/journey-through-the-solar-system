import './style.css';
import {
  addStars,
  animateTitle,
  horizontalScroll,
  moveStars,
  moveToNext,
} from './startPage';

animateTitle();
addStars();
moveStars();
moveToNext();
horizontalScroll();
